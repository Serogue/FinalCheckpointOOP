using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlienShip1 : AlienShip // INHERITANCE
{





    // Start is called before the first frame update
    void Start()
    {
        health = 3;
        speedX = 5;
        speedY = 0.5f;
    }

    // Update is called once per frame


    

    
}
